<form method="post" action="{{ path('app_comment_delete', {'id': comments[0].id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comments[0].id) }}">
    <button class="btn">Delete</button>
</form>
